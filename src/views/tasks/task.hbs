<div class="row mt-4" >
    <div class="col-md-4 offset-md-4">
        <div class="card">
            <div class="card-body">
                <div class="card-header mb-3 text-center">
                    {{#if task._id}} Editar {{else}} Nueva {{/if}}
                </div>
                {{#if task._id}} 
                    {{> _edit_task }}
                {{else}} 
                    {{> _create_task }}
                {{/if}}
            </div>
        </div>
    </div>
</div>